1. 左侧菜单栏尽量使用文字按钮，文字可以左侧加一些icon，目前项目还没有导入icon，先用方块占位
2. 下面会话历史栏也要用文字展示的形式即可，只展示预览内容，超出宽度省略号即可
   1. 历史栏中每条数据hover时的颜色和选中状态参考chatgpt,hover 颜色为变量--menu-item-highlighted: #0000000a;
    --menu-item-active: #0000000f;
    --menu-item-open: #00000006;
   2. 每项的高度为calc(var(--spacing) * 9);  --spacing： 0.25rem
3. 左侧增加弹出收回按钮，可以模仿chatgpt左侧弹出收回按钮的方式
   1. 收回后的状态， 左侧只显示一条，只显示新聊天和搜索的icon
   2. 展开后回到原来状态
